require "spec_helper"

describe Article do
    it "orders them in reverse chronologically"
    #   post = Post.create!
    #   comment1 = post.comments.create!(:body => "first comment")
    #   comment2 = post.comments.create!(:body => "second comment")
    #   expect(post.reload.comments).to eq([comment2, comment1])
    # end
end